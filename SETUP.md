# Portfolio Project - Quick Start Guide

## 🚀 Быстрый старт

### Вариант 1: Запуск всего одной командой
```bash
# Windows
start-all.bat

# Это запустит:
# - Backend сервер на http://localhost:3001
# - Frontend приложение на http://localhost:5173
```

### Вариант 2: Запуск вручную

#### Шаг 1: Запустите Backend
```bash
# В первом терминале
cd server
npm install
npm start
```

#### Шаг 2: Запустите Frontend
```bash
# Во втором терминале (из корня проекта)
npm install
npm run dev
```

## 🔐 Доступ к Admin панели

1. Откройте браузер: `http://localhost:5173/login`
2. Введите пароль: `admin123`
3. Управляйте проектами через удобный интерфейс

## 📝 Добавление нового проекта

### Через Admin панель (рекомендуется):
1. Перейдите на `/login`
2. Войдите с паролем `admin123`
3. Заполните форму:
   - Название
   - Описание
   - Детальное описание (список)
   - Изображение проекта
   - Теги технологий
4. Нажмите "Создать проект"

### Через API:
```bash
curl -X POST http://localhost:3001/api/projects \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My Project",
    "description": "Project description",
    "subDescription": ["Feature 1", "Feature 2"],
    "image": "/assets/projects/project.jpg",
    "tags": [
      {"id": 1, "name": "React", "path": "/assets/logos/react.svg"}
    ],
    "href": "https://example.com"
  }'
```

## 🛠 Структура данных проекта

```json
{
  "title": "Название проекта",
  "description": "Краткое описание",
  "subDescription": [
    "Первый пункт детального описания",
    "Второй пункт детального описания"
  ],
  "image": "/assets/projects/image.jpg",
  "href": "https://project-url.com",
  "tags": [
    {
      "id": 1,
      "name": "React",
      "path": "/assets/logos/react.svg"
    }
  ]
}
```

## 📌 Важные URL

- **Главная страница**: http://localhost:5173
- **Admin панель**: http://localhost:5173/admin
- **Вход**: http://localhost:5173/login
- **API**: http://localhost:3001/api/projects

## 🔧 Изменение пароля админа

Отредактируйте файл `src/pages/LoginPage.jsx`:
```javascript
const ADMIN_PASSWORD = "ваш_новый_пароль";
```

## 💾 База данных

Проекты хранятся в файле `server/db.json`. Можно редактировать вручную, но рекомендуется использовать Admin панель.

## 🐛 Решение проблем

### Backend не запускается
```bash
cd server
npm install express cors
npm start
```

### Frontend не видит проекты
1. Убедитесь, что backend запущен на порту 3001
2. Проверьте консоль браузера на наличие CORS ошибок
3. Попробуйте перезапустить оба сервера

### Ошибка "Cannot find module"
```bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
```

## 📦 Деплой

### Frontend (Vercel/Netlify)
- Добавьте переменную окружения `VITE_API_URL` с адресом вашего backend
- Обновите `API_URL` в компонентах

### Backend (Render/Railway/Heroku)
- Добавьте `PORT` переменную окружения
- База данных автоматически сохраняется в `db.json`

## ✨ Полезные команды

```bash
# Проверить статус сервера
curl http://localhost:3001/api/projects

# Остановить все процессы
Ctrl + C в каждом терминале

# Очистить кэш
npm cache clean --force
```
