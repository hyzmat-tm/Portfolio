# 🔐 Руководство по безопасности

## Настройка переменных окружения

### Frontend (.env)

Создайте файл `.env` в корне проекта:

```env
# API Configuration
VITE_API_URL=http://localhost:3001

# Admin Password
VITE_ADMIN_PASSWORD=ваш_надежный_пароль

# EmailJS (опционально)
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key
```

### Backend (server/.env)

Создайте файл `.env` в папке `server/`:

```env
# Server Configuration
PORT=3001
NODE_ENV=development

# Admin Password
ADMIN_PASSWORD=ваш_надежный_пароль

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
```

---

## 🔒 Важные правила безопасности

### 1. Не коммитьте .env файлы
✅ `.env` файлы уже добавлены в `.gitignore`
✅ Используйте `.env.example` как шаблон
❌ **Никогда** не публикуйте реальные пароли в репозитории

### 2. Используйте надежные пароли

```bash
# Плохо
VITE_ADMIN_PASSWORD=123456
VITE_ADMIN_PASSWORD=admin

# Хорошо
VITE_ADMIN_PASSWORD=MyStr0ng!P@ssw0rd2024
VITE_ADMIN_PASSWORD=Secure_Admin_Portal_2024!
```

### 3. Разные пароли для разных сред

```env
# Development
VITE_ADMIN_PASSWORD=dev_password_123

# Production
VITE_ADMIN_PASSWORD=production_secure_password_xyz
```

---

## 📝 Инструкция по первому запуску

### Шаг 1: Скопируйте примеры

```bash
# Frontend
copy .env.example .env

# Backend
copy server\.env.example server\.env
```

### Шаг 2: Измените пароли

Отредактируйте файлы `.env` и установите свои пароли:

**Frontend (.env):**
```env
VITE_ADMIN_PASSWORD=МойСуперПароль2024
```

**Backend (server/.env):**
```env
ADMIN_PASSWORD=МойСуперПароль2024
```

⚠️ **Важно:** Пароли должны совпадать!

### Шаг 3: Перезапустите серверы

```bash
# Остановите (Ctrl+C) и запустите заново
start-all.bat
```

---

## 🚀 Деплой в продакшен

### Для Vercel/Netlify (Frontend)

Добавьте переменные окружения в панели управления:

```
VITE_API_URL=https://your-api.com
VITE_ADMIN_PASSWORD=ваш_продакшн_пароль
```

### Для Render/Railway/Heroku (Backend)

Добавьте переменные окружения:

```
PORT=3001
NODE_ENV=production
ADMIN_PASSWORD=ваш_продакшн_пароль
CORS_ORIGIN=https://your-frontend.com
```

---

## 🛡️ Дополнительные меры безопасности

### 1. Rate Limiting (ограничение запросов)

Добавьте в `server/package.json`:
```json
"dependencies": {
  "express-rate-limit": "^7.1.0"
}
```

В `server/server.js`:
```javascript
import rateLimit from 'express-rate-limit';

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 минут
  max: 100 // максимум 100 запросов
});

app.use('/api/', limiter);
```

### 2. Helmet (HTTP заголовки безопасности)

```bash
cd server
npm install helmet
```

```javascript
import helmet from 'helmet';
app.use(helmet());
```

### 3. JWT Токены (рекомендуется для продакшена)

Замените простую аутентификацию на JWT:
```bash
npm install jsonwebtoken bcrypt
```

### 4. HTTPS в продакшене

✅ Всегда используйте HTTPS для продакшн версии
✅ Настройте SSL сертификаты (Let's Encrypt бесплатно)

---

## ⚠️ Что НЕ нужно делать

❌ Не храните пароли в коде
❌ Не коммитьте .env файлы
❌ Не используйте слабые пароли
❌ Не используйте один пароль везде
❌ Не публикуйте API ключи в открытом доступе

---

## 🔍 Проверка безопасности

### Чек-лист перед деплоем:

- [ ] `.env` файлы в `.gitignore`
- [ ] Надежные пароли установлены
- [ ] Разные пароли для dev/production
- [ ] CORS настроен правильно
- [ ] API URL обновлен для продакшена
- [ ] Секретные ключи не в коде
- [ ] HTTPS включен в продакшене

---

## 📞 Что делать при утечке пароля?

1. **Немедленно** измените пароль в `.env`
2. Перезапустите все серверы
3. Проверьте логи на подозрительную активность
4. Если код в GitHub - **смените пароль** и создайте новый коммит
5. Рассмотрите использование более безопасной аутентификации (JWT, OAuth)

---

## 📚 Дополнительные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/) - главные уязвимости
- [Express Security Best Practices](https://expressjs.com/en/advanced/best-practice-security.html)
- [Let's Encrypt](https://letsencrypt.org/) - бесплатные SSL сертификаты

---

## 💡 Текущий уровень безопасности

✅ **Реализовано:**
- Переменные окружения для паролей
- .env файлы в .gitignore
- CORS настройки
- Базовая аутентификация

⚠️ **Рекомендуется для продакшена:**
- JWT токены
- Rate limiting
- Helmet middleware
- HTTPS
- Логирование попыток входа
- Двухфакторная аутентификация (2FA)

---

**Помните:** Безопасность - это процесс, а не конечное состояние! 🛡️
