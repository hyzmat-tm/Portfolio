# 📧 Настройка SMTP для отправки писем

## Настройка Mail.ru для отправки писем

### Шаг 1: Получите пароль приложения

1. Войдите в ваш аккаунт Mail.ru
2. Перейдите: **Настройки** → **Безопасность** → **Пароли для внешних приложений**
3. Или прямая ссылка: https://account.mail.ru/user/2-step-auth/passwords/
4. Создайте новый пароль для приложения:
   - Название: "Portfolio Website"
   - Скопируйте сгенерированный пароль

### Шаг 2: Настройте переменные окружения

Откройте файл `server/.env` и заполните:

```env
# SMTP Configuration (Mail.ru)
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=ваш-email@mail.ru
SMTP_PASS=пароль-приложения-из-шага-1
RECIPIENT_EMAIL=email-куда-отправлять@mail.ru
```

**Пример:**
```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=atajan@mail.ru
SMTP_PASS=AbCdEfGh1234567890
RECIPIENT_EMAIL=atajan@mail.ru
```

### Шаг 3: Установите зависимости

```bash
cd server
npm install nodemailer
```

### Шаг 4: Перезапустите сервер

```bash
npm start
```

---

## Использование других почтовых сервисов

### Gmail

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=app-password-from-google
RECIPIENT_EMAIL=your-email@gmail.com
```

**Важно для Gmail:**
1. Включите двухфакторную аутентификацию
2. Создайте пароль приложения: https://myaccount.google.com/apppasswords

### Yandex

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.ru
SMTP_PASS=your-app-password
RECIPIENT_EMAIL=your-email@yandex.ru
```

### Rambler

```env
SMTP_HOST=smtp.rambler.ru
SMTP_PORT=465
SMTP_USER=your-email@rambler.ru
SMTP_PASS=your-password
RECIPIENT_EMAIL=your-email@rambler.ru
```

---

## Тестирование

### 1. Проверьте, что сервер запущен

```bash
curl http://localhost:3001/api/projects
```

### 2. Отправьте тестовое письмо

Откройте форму контактов на сайте: `http://localhost:5173/#contact`

Заполните:
- **Имя:** Test User
- **Email:** test@example.com
- **Сообщение:** Тестовое сообщение

Нажмите "Send Message"

### 3. Проверьте почту

Письмо должно прийти на адрес, указанный в `RECIPIENT_EMAIL`

---

## Решение проблем

### Ошибка: "Authentication failed"

**Причина:** Неверный пароль приложения

**Решение:**
1. Проверьте, что используете **пароль приложения**, а не обычный пароль
2. Пересоздайте пароль приложения
3. Проверьте отсутствие пробелов в `.env` файле

### Ошибка: "Connection timeout"

**Причина:** Порт заблокирован или неверный хост

**Решение:**
1. Проверьте настройки файрвола
2. Убедитесь, что используете порт 465 (SSL) или 587 (TLS)
3. Для Mail.ru используйте `smtp.mail.ru`

### Ошибка: "Self-signed certificate"

**Решение:** Добавьте в transporter:
```javascript
tls: {
  rejectUnauthorized: false
}
```

### Письма не приходят (но ошибок нет)

**Проверьте:**
1. Папку "Спам" / "Нежелательная почта"
2. Правильность `RECIPIENT_EMAIL`
3. Логи сервера: `console.log` в endpoint

---

## Логирование для отладки

Добавьте в `server/server.js`:

```javascript
app.post('/api/send-email', async (req, res) => {
  console.log('📧 Incoming email request:', req.body);

  try {
    // ... existing code

    const info = await transporter.sendMail(mailOptions);
    console.log('✅ Email sent:', info.messageId);

    res.json({ message: 'Email sent successfully!' });
  } catch (error) {
    console.error('❌ Email error:', error);
    // ... error handling
  }
});
```

---

## Безопасность

✅ **Правильно:**
- Пароли в `.env` файле
- `.env` в `.gitignore`
- Пароли приложений вместо основных
- HTTPS в продакшене

❌ **Неправильно:**
- Пароли в коде
- Коммит `.env` в Git
- Использование основного пароля
- HTTP для отправки данных

---

## Преимущества SMTP вместо EmailJS

✅ **Плюсы:**
- Бесплатно и без лимитов
- Полный контроль над письмами
- Нет зависимости от внешних сервисов
- Можно настроить любой дизайн письма
- Работает с любым почтовым сервисом
- Нет необходимости регистрации на сторонних платформах

❌ **Минусы EmailJS:**
- Лимит на количество писем
- Зависимость от стороннего сервиса
- Может быть платным при большом объеме
- Меньше контроля над процессом

---

## API Endpoint

**POST** `/api/send-email`

**Request Body:**
```json
{
  "name": "John Doe",
  "email": "john@example.com",
  "message": "Hello, I'm interested in your services!"
}
```

**Success Response (200):**
```json
{
  "message": "Email sent successfully!"
}
```

**Error Response (500):**
```json
{
  "error": "Failed to send email",
  "details": "Error message (только в development)"
}
```

---

## Готово! 🎉

Теперь форма контактов работает через ваш собственный SMTP сервер Mail.ru!

**Не забудьте:**
1. ✅ Заполнить `server/.env` настройками
2. ✅ Установить `nodemailer`
3. ✅ Перезапустить backend сервер
4. ✅ Протестировать отправку письма
